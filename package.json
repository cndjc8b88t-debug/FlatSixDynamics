{
  "name": "flatsixdynamics-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Marketing site for FlatSixDynamics. Eleventy + GitHub Pages, no JS framework, no tracker.",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve --port=8081",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
